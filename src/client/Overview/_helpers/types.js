export type Overall = {
	title: string,
	users: number,
	score: number,
	id: string,
	desc: string
};

export type Essay = {
	title: string,
	users: number,
	score: number,
	id: string,
	desc: string
};

export type Performance = {
	name: string,
	data: Array<number>
};
